rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir solo a usuarios autenticados. En producción limita por colección/camino.
      allow read, write: if request.auth != null;
    }
  }
}
